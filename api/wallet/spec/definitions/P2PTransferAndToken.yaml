type: object
required:
  - p2pTransfer
  - token
properties:
  p2pTransfer:
    $ref: '#/definitions/P2PTransfer'
  token:
    description: >
      Токен доступа к переводу создается в результате операции создания
      перевода. Он авторизует:

      * получение перевода по его идентификатору
    $ref: '#/definitions/GrantToken'
