type: object
required:
  - templateGrant
properties:
  body:
    x-rebillyMerge:
      - $ref: '#/definitions/Asset'
      - description: Сумма перевода
  metadata:
    type: object
    description: 'Метаданные, которые необходимо связать с переводом. **Объединяются с метаданными шаблона**'
  templateGrant:
    x-rebillyMerge:
      - $ref: '#/definitions/GrantToken'
      - description: |
          Токен, дающий право создания перевода по шаблону.
  idempotencyGrant:
    x-rebillyMerge:
      - $ref: '#/definitions/GrantToken'
      - description: |
          Токен, обеспечивающий идемпотентное создание перевода по шаблону.
