description: Данные перевода
type: object
required:
  - id
properties:
  id:
    x-rebillyMerge:
      - $ref: '#/definitions/P2PTransferID'
      - readOnly: true
  createdAt:
    description: Дата и время создания
    type: string
    format: date-time
  total:
    x-rebillyMerge:
      - $ref: '#/definitions/Asset'
      - description: Общая сумма перевода
  sender:
    $ref: '#/definitions/SenderResource'
  receiver:
    $ref: '#/definitions/ReceiverResource'
  status:
    $ref: '#/definitions/P2PTransferStatus'
  externalID:
    x-rebillyMerge:
      - $ref: '#/definitions/ExternalID'
  metadata:
    description: Связанные с переводом метаданные
    type: object
