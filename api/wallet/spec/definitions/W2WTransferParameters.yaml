description: Параметры создания перевода
type: object
required:
  - sender
  - receiver
  - identityID
  - body
properties:
  sender:
    $ref: '#/definitions/WalletID'
  receiver:
    $ref: '#/definitions/WalletID'
  identityID:
    x-rebillyMerge:
      - $ref: '#/definitions/IdentityID'
  body:
    x-rebillyMerge:
      - $ref: '#/definitions/Asset'
      - description: Сумма перевода
  metadata:
    description: 'Метаданные, которые необходимо связать с переводом'
    type: object
  externalID:
    x-rebillyMerge:
      - $ref: '#/definitions/ExternalID'
  walletGrant:
    x-rebillyMerge:
      - $ref: '#/definitions/GrantToken'
      - description: |
          Токен, дающий право на списание с кошелька.

          Необходимо предоставить в том случае, если перевод производится засчёт средств _чужого_
          кошелька. Владелец указанного кошелька может
          [выдать на это право](#operation/issueWalletGrant).
