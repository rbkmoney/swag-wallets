type: object
allOf:
  - $ref: '#/definitions/TransferChange'
  - $ref: '#/definitions/TransferStatus'
  - type: object
    required:
      - paymentID
    properties:
      paymentID:
        type: string
