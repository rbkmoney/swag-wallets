type: object
allOf:
  - $ref: '#/definitions/P2PTransferChange'
  - type: object
    description: |
      Требование провести взаимодействие с плательщиком для продолжения процесса
      перевода
    required:
      - userInteractionID
      - userInteractionIntent
    properties:
      userInteractionID:
        description: Идентификатор взаимодействия с пользователем
        type: string
      userInteractionIntent:
        $ref: '#/definitions/UserInteractionIntent'
