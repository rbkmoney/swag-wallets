type: object
discriminator: intentType
required:
  - intentType
properties:
  intentType:
    description: Вид намеререния по взаимодействию с пользователем.
    type: string
    enum:
      - UserInteractionIntentCreate
      - UserInteractionIntentFinish
