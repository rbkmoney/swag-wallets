type: object
required:
  - intentType
properties:
  intentType:
    description: Требуется ли взаимодействия с пользователем или более не требуется.
    type: string
    enum:
      - create
      - finish
  userInteraction:
    $ref: '#/definitions/UserInteraction'
