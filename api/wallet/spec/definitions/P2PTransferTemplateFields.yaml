type: object
description: Поля по умолчанию шаблона перевода
properties:
  description:
    description: Описание перевода
    type: string
    maxLength: 1000
  templateBody:
    x-rebillyMerge:
      - $ref: '#/definitions/P2PTransferTemplateBody'
  templateMetadata:
    x-rebillyMerge:
      - $ref: '#/definitions/P2PTransferTemplateMetadata'
