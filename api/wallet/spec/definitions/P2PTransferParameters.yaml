description: Параметры создания перевода
type: object
required:
  - sender
  - receiver
  - senderIdentityID
  - body
properties:
  sender:
    $ref: '#/definitions/SenderResource'
  receiver:
    $ref: '#/definitions/ReceiverResource'
  senderIdentityID:
    $ref: '#/definitions/IdentityID'
  body:
    x-rebillyMerge:
      - $ref: '#/definitions/Asset'
      - description: Сумма перевода
  feeToken:
    description: Токен полученный из [calculateFee](#operation/calculateFee)
    type: string
    minLength: 1
    maxLength: 4000
  metadata:
    description: 'Метаданные, которые необходимо связать с переводом'
    type: object
  externalID:
    x-rebillyMerge:
      - $ref: '#/definitions/ExternalID'
