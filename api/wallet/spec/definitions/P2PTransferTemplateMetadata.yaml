type: object
required:
  - metadata
properties:
  metadata:
    description: >
      Метаданные, которые будут связаны с переводом, созданным по шаблону, в
      случае, если иные метаданные не указаны в запросе на создание перевода.
    type: object
