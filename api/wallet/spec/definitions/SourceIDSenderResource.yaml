description: Идентификатор источника
allOf:
  - $ref: '#/definitions/SenderResource'
  - type: object
    required:
      - sourceID
    properties:
      sourceID:
        x-rebillyMerge:
          - $ref: '#/definitions/SourceID'
