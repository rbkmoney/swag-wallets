type: object
description: Детали шаблона перевода
properties:
  count:
    description: |
      Количество переводов, которые можно создать по этому шаблону
    type: integer
    format: int64
    example: 1430000
  body:
    x-rebillyMerge:
      - $ref: '#/definitions/Asset'
      - description: Сумма перевода
