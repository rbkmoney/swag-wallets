type: object
required:
  - status
properties:
  status:
    description: Статус перевода
    type: string
    enum:
      - pending
      - processed
      - cancelled
      - failed
  error:
    $ref: '#/definitions/TransferError'
