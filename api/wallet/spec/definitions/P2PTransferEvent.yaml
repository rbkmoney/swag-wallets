type: object
required:
  - id
  - createdAt
  - changes
properties:
  createdAt:
    type: string
    format: date-time
  changes:
    type: array
    items:
      $ref: '#/definitions/P2PTransferChange'
