type: object
required:
  - identityID
  - fields
properties:
  identityID:
    x-rebillyMerge:
      - $ref: '#/definitions/IdentityID'
  fields:
    $ref: '#/definitions/P2PTransferTemplateFields'
  externalID:
    x-rebillyMerge:
      - $ref: '#/definitions/ExternalID'
