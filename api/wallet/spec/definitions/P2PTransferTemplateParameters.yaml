type: object
required:
  - identityID
  - fields
properties:
  externalID:
    x-rebillyMerge:
      - $ref: '#/definitions/ExternalID'
  identityID:
    x-rebillyMerge:
      - $ref: '#/definitions/IdentityID'
  fields:
    $ref: '#/definitions/P2PTransferTemplateFields'
