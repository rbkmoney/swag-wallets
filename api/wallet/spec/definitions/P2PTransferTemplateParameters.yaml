type: object
required:
  - identityID
  - details
properties:
  identityID:
    x-rebillyMerge:
      - $ref: '#/definitions/IdentityID'
  details:
    $ref: '#/definitions/P2PTransferTemplateDetails'
  externalID:
    x-rebillyMerge:
      - $ref: '#/definitions/ExternalID'
