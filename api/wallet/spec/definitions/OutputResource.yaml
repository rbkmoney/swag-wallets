description: Ресурс для осуществления выводов
type: object
discriminator: type
required:
  - type
properties:
  type:
    description: |
      Тип ресурса для выводов.

      См. [RBKmoney Payment Resource API](/?api/payres/swagger.yaml).
    type: string
    enum:
      - SecurePaymentResource
