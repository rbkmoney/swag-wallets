type: object
required:
  - status
properties:
  status:
    description: Статус перевода
    type: string
    enum:
      - pending
      - processed
      - failed
  error:
    $ref: '#/definitions/P2PTransferError'
