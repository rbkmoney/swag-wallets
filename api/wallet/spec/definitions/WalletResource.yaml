description: Кошелёк как платёжный ресурс
allOf:
  - $ref: '#/definitions/PaymentResource'
  - type: object
    required:
      - walletID
    properties:
      type:
        description: Кошелёк
        type: string
        enum: [WalletResource]
      walletID:
        x-rebillyMerge:
          - $ref: '#/definitions/WalletID'
      walletGrant:
        x-rebillyMerge:
          - $ref: '#/definitions/WalletGrantToken'
