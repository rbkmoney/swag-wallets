get:
  operationId: getProviderCurrencyTerms
  summary: Получить валютные условия провайдера
  tags:
    - Providers
  parameters:
    - $ref: '#/parameters/requestID'
    - $ref: '#/parameters/providerID'
  responses:
    '200':
      description: Условия расчитаны
      schema:
        $ref: '#/definitions/TermsCurrency'
    '404':
      $ref: '#/responses/NotFound'
    '400':
      $ref: '#/responses/BadRequest'
