description: Данные провайдера услуг
type: object
required:
  - id
  - name
  - servicedResidences
properties:
  id:
    x-rebillyMerge:
      - $ref: '#/definitions/ProviderID'
  name:
    description: |
      Человекочитаемое наименование провайдера услуг
    type: string
    example: НКО «ЭПС»
  servicedResidences:
    description: |
      Список резиденций, в которых провайдер имеет право предоставлять услуги
    type: array
    items:
      x-rebillyMerge:
        - $ref: '#/definitions/Residence'
