description: Исходные данные подтверждающего личность документа
type: object
discriminator: type
required:
  - type
properties:
  type:
    description: Вид данных подтверждающего личность документа
    type: string
    enum:
      - RUSDomesticPassportData
      - RUSRetireeInsuranceCertificateData
