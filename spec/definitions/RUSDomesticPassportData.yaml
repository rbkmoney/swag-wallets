description: Исходные данные национального паспорта гражданина РФ
allOf:
  - $ref: '#/definitions/IdentityProofDocument'
  - type: object
    required:
      - series
      - number
      - issuer
      - familyName
      - firstName
      - patronymic
    properties:
      type:
        description: Национальный паспорт гражданина РФ
        type: string
      series:
        description: Серия паспорта
        type: string
        example: "4567"
        pattern: '^[0-9]{4}$'
      number:
        description: Номер паспорта
        type: string
        example: "123456"
        pattern: '^[0-9]{6}$'
      issuer:
        description: Наименование выдавшего паспорт органа
        type: string
        example: |
          Отделение УФМС России по Кемеровской области в Упоровском районе
        maxLength: 1000
      familyName:
        description: Фамилия гражданина
        type: string
        example: "Иванов"
        pattern: "^[А-Яа-я'-]{1,100}$"
      firstName:
        description: Имя гражданина
        type: string
        example: "Иван"
        pattern: "^[А-Яа-я'-]{1,100}$"
      patronymic:
        description: Отчество гражданина
        type: string
        example: "Иванович"
        pattern: "^[А-Яа-я'-]{1,100}$"
